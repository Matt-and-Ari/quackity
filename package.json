{
  "name": "quack",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
    "dev": "vp run website#dev",
    "instant:pull": "instant-cli pull",
    "instant:push": "instant-cli push schema && instant-cli push perms",
    "prepare": "vp config"
  },
  "devDependencies": {
    "instant-cli": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
